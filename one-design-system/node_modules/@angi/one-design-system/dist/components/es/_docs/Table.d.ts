import { ReactNode } from 'react';
type RowData = {
    [key: string]: unknown;
};
export interface TableColumn<T extends RowData = RowData> {
    /**
     * The header text for the column
     */
    header: string;
    /**
     * The key to access the value in each row
     */
    key: string;
    /**
     * Custom cell renderer. Receives the raw value and full row object.
     */
    render?: (value: unknown, row: T) => ReactNode;
}
export interface TableProps<T extends RowData = RowData> {
    /**
     * Column definitions
     */
    columns: TableColumn<T>[];
    /**
     * Array of row data objects
     */
    data: T[];
    /**
     * When true, table width fits content instead of full width
     * @default false
     */
    compact?: boolean;
}
/**
 * @internal
 * A styled table component for Storybook MDX documentation.
 * Use for item type tables, comparison tables, or any custom tabular data.
 * For component props, use Storybook's `<Controls />` with properly defined `argTypes`.
 *
 * Not exported from the design system - only used in MDX docs.
 */
export declare function Table<T extends RowData>({ columns, data, compact, }: TableProps<T>): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=Table.d.ts.map