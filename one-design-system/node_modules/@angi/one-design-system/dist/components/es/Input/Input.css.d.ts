import { ComplexStyleRule } from '@vanilla-extract/css';
/**
 * Use when focus should be shown on the wrapper when the input element has keyboard focus
 * (e.g., input with addons)
 */
export declare const containerFocusVisible: ComplexStyleRule;
/**
 * Wrapper container for input with addons
 * Uses flexbox to layout startAddon, input, and endAddon
 */
export declare const inputWrapper: import('@vanilla-extract/recipes').RuntimeFn<{
    size: {
        sm: {
            height: "2rem";
            paddingLeft: `var(--${string})`;
            paddingRight: `var(--${string})`;
            paddingTop: `var(--${string})`;
            paddingBottom: `var(--${string})`;
        };
        md: {
            height: "3rem";
            paddingLeft: `var(--${string})`;
            paddingRight: `var(--${string})`;
            paddingTop: `var(--${string})`;
            paddingBottom: `var(--${string})`;
        };
    };
    isDisabled: {
        true: {
            cursor: "not-allowed";
            backgroundColor: `var(--${string})`;
            borderColor: `var(--${string})`;
        };
        false: {};
    };
    isReadOnly: {
        true: {
            backgroundColor: `var(--${string})`;
        };
        false: {};
    };
}>;
/**
 * Inner input element styles (when used with wrapper)
 * Border and background handled by wrapper
 */
export declare const inputInner: string;
/**
 * Addon container styles (for start/end addons)
 */
export declare const addonContainer: string;
/**
 * Search icon styles
 */
export declare const searchIcon: string;
/**
 * Standalone input styles (when used without wrapper/addons)
 * Includes full border, background, and focus styles
 */
export declare const input: import('@vanilla-extract/recipes').RuntimeFn<{
    size: {
        sm: {
            height: "2rem";
            paddingLeft: `var(--${string})`;
            paddingRight: `var(--${string})`;
            paddingTop: `var(--${string})`;
            paddingBottom: `var(--${string})`;
        };
        md: {
            height: "3rem";
            paddingLeft: `var(--${string})`;
            paddingRight: `var(--${string})`;
            paddingTop: `var(--${string})`;
            paddingBottom: `var(--${string})`;
        };
    };
}>;
//# sourceMappingURL=Input.css.d.ts.map