export declare const tile: import('@vanilla-extract/recipes').RuntimeFn<{
    size: {
        xs: {
            padding: `var(--${string})`;
        };
        sm: {
            padding: `var(--${string})`;
        };
        md: {
            padding: `var(--${string})`;
        };
        lg: {
            padding: `var(--${string})`;
        };
    };
}>;
//# sourceMappingURL=Tile.css.d.ts.map