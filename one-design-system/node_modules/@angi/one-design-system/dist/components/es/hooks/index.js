import { u as O } from "../_chunks/useTranslations-Da7GR3Ot.js";
import { useState as u, useEffect as l } from "react";
import { b as a } from "../_chunks/tokens-DMvC-b0j.js";
const d = Object.entries(a).sort(([, t], [, o]) => parseFloat(t) - parseFloat(o)).map(([t]) => t);
function E(t, o) {
  const [m, s] = u(t), c = JSON.stringify(o);
  return l(() => {
    const i = d.filter(
      (e) => e in o && o[e] !== void 0
    );
    if (i.length === 0) {
      s(t);
      return;
    }
    const r = i.map((e) => ({
      breakpoint: e,
      mql: window.matchMedia(`(min-width: ${a[e]})`)
    })), n = () => {
      let e = t;
      for (const { breakpoint: p, mql: f } of r)
        f.matches && (e = o[p]);
      s(e);
    };
    n();
    for (const { mql: e } of r)
      e.addEventListener("change", n);
    return () => {
      for (const { mql: e } of r)
        e.removeEventListener("change", n);
    };
  }, [t, c]), m;
}
export {
  E as useResponsiveProp,
  O as useTranslations
};
//# sourceMappingURL=index.js.map
