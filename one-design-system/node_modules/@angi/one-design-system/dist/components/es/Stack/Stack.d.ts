import { HTMLAttributes, ReactNode } from 'react';
import { StackLayoutProps } from './Stack.sprinkles';
interface StackCustomProps extends StackLayoutProps {
    /**
     * When true, renders the child element instead of a div,
     * merging Stack's props and behavior onto the child.
     */
    asChild?: boolean;
    children?: ReactNode;
}
export interface StackProps extends Omit<HTMLAttributes<HTMLDivElement>, keyof StackCustomProps>, StackCustomProps {
}
export declare const Stack: import('react').ForwardRefExoticComponent<StackProps & import('react').RefAttributes<HTMLDivElement>>;
export {};
//# sourceMappingURL=Stack.d.ts.map