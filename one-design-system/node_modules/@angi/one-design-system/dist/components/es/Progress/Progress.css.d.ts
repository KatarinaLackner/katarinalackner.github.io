export declare const root: string;
/**
 * Visually hidden value text for screen readers only.
 * Announces the current progress value to assistive technologies.
 */
export declare const valueText: string;
export declare const track: import('@vanilla-extract/recipes').RuntimeFn<{
    size: {
        sm: {
            height: "4px";
        };
        lg: {
            height: "8px";
        };
    };
    variant: {
        solid: {
            backgroundColor: `var(--${string})`;
        };
        outline: {
            backgroundColor: "transparent";
            borderWidth: `var(--${string})`;
            borderStyle: "solid";
            borderColor: `var(--${string})`;
            boxSizing: "border-box";
        };
    };
    radius: {
        none: {
            borderRadius: `var(--${string})`;
        };
        sm: {
            borderRadius: `var(--${string})`;
        };
    };
}>;
export declare const fill: import('@vanilla-extract/recipes').RuntimeFn<{
    radius: {
        none: {
            borderRadius: `var(--${string})`;
        };
        sm: {
            borderRadius: `var(--${string})`;
        };
    };
    animation: {
        true: {
            transition: "width 0.3s ease-out";
        };
    };
}>;
//# sourceMappingURL=Progress.css.d.ts.map