import { RadioButtonTileProps } from '../RadioButtonTile';
import { RadioButtonProps } from '../RadioButton';
import { RadioButtonCompactProps } from './components/_RadioButtonCompact';
export declare const RadioButtonFieldsetVariants: readonly ["tile", "text", "compact"];
export type RadioButtonFieldsetVariant = (typeof RadioButtonFieldsetVariants)[number];
export declare const DEFAULT_RADIO_BUTTON_FIELDSET_VARIANT: RadioButtonFieldsetVariant;
/**
 * Item shape for the tile variant.
 * Uses RadioButtonTile internally.
 */
export type TileItem = Omit<RadioButtonTileProps, "hideFocusRing">;
/**
 * Item shape for the text variant.
 * Uses RadioButton internally.
 */
export type TextItem = Omit<RadioButtonProps, "hideFocusRing">;
/**
 * Item shape for the compact variant.
 * Uses internal _RadioButtonCompact component.
 */
export type CompactItem = Omit<RadioButtonCompactProps, "hideFocusRing">;
//# sourceMappingURL=RadioButtonFieldset.config.d.ts.map