/**
 * Outer fieldset container (holds legend/hint stack + inputs + validation)
 * Includes reset styles for native fieldset element
 */
export declare const fieldset: string;
/**
 * Legend with cursor pointer to indicate it's clickable
 */
export declare const clickableLegend: string;
/**
 * Stack for legend and hint with tighter spacing
 */
export declare const legendHintStack: string;
/**
 * Container for the OTP input cells
 */
export declare const container: import('@vanilla-extract/recipes').RuntimeFn<{
    size: {
        sm: {
            gap: `var(--${string})`;
        };
        md: {
            gap: `var(--${string})`;
        };
        lg: {
            gap: `var(--${string})`;
        };
    };
}>;
/**
 * OTP input cell overrides for the Input component.
 * Overrides width (fixed instead of 100%) and text alignment (centered).
 * All other styling (border, focus, validation, disabled) comes from Input.
 *
 * Uses && (double ampersand) to increase specificity and ensure these styles
 * override Input's styles regardless of CSS source order.
 */
export declare const inputCell: import('@vanilla-extract/recipes').RuntimeFn<{
    size: {
        sm: {
            selectors: {
                "&&": {
                    width: `var(--${string})`;
                };
            };
        };
        md: {
            selectors: {
                "&&": {
                    width: `var(--${string})`;
                };
            };
        };
        lg: {
            selectors: {
                "&&": {
                    width: `var(--${string})`;
                    height: `var(--${string})`;
                };
            };
        };
    };
}>;
//# sourceMappingURL=OTPFieldset.css.d.ts.map