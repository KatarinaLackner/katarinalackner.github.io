import { HTMLAttributes, ReactNode } from 'react';
export interface DockSheetCustomProps {
    /**
     * The content to display inside the dock sheet
     */
    children: ReactNode;
}
export interface DockSheetProps extends Omit<HTMLAttributes<HTMLDivElement>, keyof DockSheetCustomProps>, DockSheetCustomProps {
}
/**
 * A container component that is fixed at the bottom of the viewport.
 * Typically used for sticky action bars, navigation, or contextual actions.
 */
export declare const DockSheet: import('react').ForwardRefExoticComponent<DockSheetProps & import('react').RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=DockSheet.d.ts.map