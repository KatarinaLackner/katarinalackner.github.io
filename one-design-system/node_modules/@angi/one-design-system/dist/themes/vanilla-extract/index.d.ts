export declare const theme: string;

export declare const themeContract: {
    borderRadius: {
        "2xl": `var(--${string})`;
        "3xl": `var(--${string})`;
        default: `var(--${string})`;
        full: `var(--${string})`;
        lg: `var(--${string})`;
        md: `var(--${string})`;
        none: `var(--${string})`;
        sm: `var(--${string})`;
        xl: `var(--${string})`;
    };
    borderWidth: {
        "1": `var(--${string})`;
        "2": `var(--${string})`;
        "4": `var(--${string})`;
        "8": `var(--${string})`;
        none: `var(--${string})`;
    };
    breakpoints: {
        "2xs": `var(--${string})`;
        "2xl": `var(--${string})`;
        lg: `var(--${string})`;
        md: `var(--${string})`;
        sm: `var(--${string})`;
        xl: `var(--${string})`;
        xs: `var(--${string})`;
    };
    color: {
        bg: {
            base: {
                active: `var(--${string})`;
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
                readonly: `var(--${string})`;
            };
            checked: {
                default: `var(--${string})`;
                hover: `var(--${string})`;
            };
            danger: {
                solid: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            info: {
                solid: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            neutral: {
                solid: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            overlay: {
                default: `var(--${string})`;
            };
            page: {
                default: `var(--${string})`;
            };
            primary: {
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            secondary: {
                solid: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            section: {
                neutral: {
                    subtle: {
                        default: `var(--${string})`;
                    };
                };
                primary: {
                    subtle: {
                        default: `var(--${string})`;
                    };
                };
                white: {
                    default: `var(--${string})`;
                };
            };
            success: {
                solid: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            warning: {
                solid: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
        };
        black: `var(--${string})`;
        border: {
            checked: {
                default: `var(--${string})`;
            };
            danger: {
                solid: {
                    default: `var(--${string})`;
                };
            };
            info: {
                solid: {
                    default: `var(--${string})`;
                };
            };
            neutral: {
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                };
            };
            primary: {
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            success: {
                solid: {
                    default: `var(--${string})`;
                };
            };
            warning: {
                solid: {
                    default: `var(--${string})`;
                };
            };
        };
        focus: {
            default: `var(--${string})`;
        };
        icon: {
            danger: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            info: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            neutral: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            primary: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            secondary: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            success: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            warning: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
        };
        text: {
            danger: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            info: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            link: {
                inverted: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                    visited: `var(--${string})`;
                };
                neutral: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    hover: `var(--${string})`;
                    visited: `var(--${string})`;
                };
            };
            neutral: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                placeholder: `var(--${string})`;
                subtle: `var(--${string})`;
            };
            primary: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            secondary: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            success: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
            warning: {
                default: `var(--${string})`;
                disabled: `var(--${string})`;
                hover: `var(--${string})`;
            };
        };
        transparent: `var(--${string})`;
        white: `var(--${string})`;
    };
    componentColor: {
        bg: {
            danger: {
                ghost: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                outline: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                solid: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            info: {
                ghost: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                outline: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                solid: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            inverted: {
                ghost: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                outline: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                solid: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            neutral: {
                ghost: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                outline: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                solid: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            primary: {
                ghost: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                outline: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                solid: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            success: {
                ghost: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                outline: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                solid: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
            warning: {
                ghost: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                outline: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                solid: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
                subtle: {
                    active: `var(--${string})`;
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                    hover: `var(--${string})`;
                };
            };
        };
        border: {
            danger: {
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            info: {
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            inverted: {
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            neutral: {
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            primary: {
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            success: {
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            warning: {
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
        };
        icon: {
            danger: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            info: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            inverted: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            neutral: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            primary: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            success: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            warning: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
        };
        text: {
            danger: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            info: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            inverted: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            neutral: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            primary: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            success: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
            warning: {
                ghost: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                outline: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                solid: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
                subtle: {
                    default: `var(--${string})`;
                    disabled: `var(--${string})`;
                };
            };
        };
    };
    components: {
        button: {
            fontWeight: `var(--${string})`;
            fontWeightSmall: `var(--${string})`;
            minWidth: `var(--${string})`;
        };
        divider: {
            color: {
                default: `var(--${string})`;
            };
        };
        input: {
            height: {
                large: `var(--${string})`;
                medium: `var(--${string})`;
                small: `var(--${string})`;
            };
            width: {
                large: `var(--${string})`;
                medium: `var(--${string})`;
                small: `var(--${string})`;
            };
        };
        modal: {
            maxHeight: {
                lg: `var(--${string})`;
                md: `var(--${string})`;
                sm: `var(--${string})`;
                xl: `var(--${string})`;
            };
            minWidth: `var(--${string})`;
            width: {
                lg: `var(--${string})`;
                md: `var(--${string})`;
                sm: `var(--${string})`;
            };
        };
        textarea: {
            minHeight: `var(--${string})`;
        };
    };
    font: {
        family: {
            body: `var(--${string})`;
            heading: `var(--${string})`;
        };
        letterSpacing: {
            loose: `var(--${string})`;
            looser: `var(--${string})`;
            normal: `var(--${string})`;
            tight: `var(--${string})`;
            tighter: `var(--${string})`;
        };
        size: {
            "2xs": `var(--${string})`;
            "2xl": `var(--${string})`;
            "3xl": `var(--${string})`;
            "4xl": `var(--${string})`;
            "5xl": `var(--${string})`;
            "6xl": `var(--${string})`;
            "7xl": `var(--${string})`;
            base: `var(--${string})`;
            lg: `var(--${string})`;
            sm: `var(--${string})`;
            xl: `var(--${string})`;
            xs: `var(--${string})`;
        };
        weight: {
            body: `var(--${string})`;
            bodyHeavy: `var(--${string})`;
            heading: `var(--${string})`;
            headingSlim: `var(--${string})`;
        };
    };
    spacing: {
        "0": `var(--${string})`;
        "2": `var(--${string})`;
        "4": `var(--${string})`;
        "6": `var(--${string})`;
        "8": `var(--${string})`;
        "12": `var(--${string})`;
        "16": `var(--${string})`;
        "20": `var(--${string})`;
        "24": `var(--${string})`;
        "28": `var(--${string})`;
        "32": `var(--${string})`;
        "36": `var(--${string})`;
        "40": `var(--${string})`;
        "44": `var(--${string})`;
        "48": `var(--${string})`;
        "56": `var(--${string})`;
        "60": `var(--${string})`;
        "64": `var(--${string})`;
        "72": `var(--${string})`;
        "80": `var(--${string})`;
        "96": `var(--${string})`;
        "128": `var(--${string})`;
        "160": `var(--${string})`;
    };
    text: {
        body: {
            "2xs": {
                heavy: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
                normal: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
            base: {
                heavy: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
                normal: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
            lg: {
                heavy: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
                normal: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
            sm: {
                heavy: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
                normal: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
            xs: {
                heavy: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
                normal: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
        };
        heading: {
            "2xl": {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            "3xl": {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            "4xl": {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            "5xl": {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            "6xl": {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            "7xl": {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            base: {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            lg: {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
            xl: {
                fontFamily: `var(--${string})`;
                fontSize: `var(--${string})`;
                fontWeight: `var(--${string})`;
                letterSpacing: `var(--${string})`;
                lineHeight: `var(--${string})`;
                textDecoration: `var(--${string})`;
                textTransform: `var(--${string})`;
            };
        };
        link: {
            base: {
                default: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
                hover: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
            sm: {
                default: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
                hover: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
        };
        list: {
            base: {
                normal: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
            sm: {
                normal: {
                    fontFamily: `var(--${string})`;
                    fontSize: `var(--${string})`;
                    fontWeight: `var(--${string})`;
                    letterSpacing: `var(--${string})`;
                    lineHeight: `var(--${string})`;
                    textDecoration: `var(--${string})`;
                    textTransform: `var(--${string})`;
                };
            };
        };
    };
};

export { }
